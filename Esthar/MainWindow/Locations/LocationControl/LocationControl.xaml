<UserControl x:Class="Esthar.LocationControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:esthar="clr-namespace:Esthar"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <TabControl>
            <esthar:LocationControlTextTab x:Name="TextTab" DataContext="{Binding}"/>
            <!--<esthar:LocationControlScriptsTab x:Name="ScriptsTab" DataContext="{Binding}"/>-->
        </TabControl>
    </Grid>
</UserControl>
